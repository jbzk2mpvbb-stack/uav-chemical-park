# ChemPark Control Center Web Demo

This is a standalone browser demo for the ChemPark hazardous-chemical transport control center.

Open `index.html` directly in a browser to view the app. Keep the `assets` folder next to `index.html`.
